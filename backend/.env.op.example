# 1Password CLI secret references (backend only)
# Copy to .env.op and replace with your op:// references. Do not commit .env.op.
# Run backend with: op run --env-file=.env.op -- python app.py
#
# Get a secret reference from 1Password (e.g. item field) and use the op:// URI format.
#
# User key: used on My Access page (list organizations for the logged-in user view).
MERAKI_USER_API_KEY=op://YourVault/YourItem/user_api_key
# Service key: used on Request Access page (organizations dropdown for granting access).
MERAKI_SERVICE_API_KEY=op://YourVault/YourItem/service_api_key
